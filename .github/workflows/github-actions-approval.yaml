name: 'Manual Approval'

on:
  push:
    branches:
      - "main"

jobs:

  deploy-dev:
    runs-on: 'ubuntu-latest'
    environment: 'dev'
    steps:

      - name: Executando no enviorment dev
        run: |
          echo [development]

  deploy-prod:
    runs-on: 'ubuntu-latest'
    environment: 'prod'
    steps:

      - name: Executando no enviorment prod
        run: |
          echo [production]
