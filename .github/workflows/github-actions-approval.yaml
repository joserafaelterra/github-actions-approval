name: 'Manual Approval'

on:
  push:
    branches:
      - "main"

jobs:

  deploy-dev:
    runs-on: 'ubuntu-latest'
    environment: 'dev'
    steps:

      - name: Run a multi-line script
        run: |
          echo [development]

  deploy-prod:
    runs-on: 'ubuntu-latest'
    environment: 'prod'
    steps:

      - name: Run a multi-line script
        run: |
          echo [production]
